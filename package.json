{
  "name": "hvostid",
  "private": true,
  "description": "Repo-wide tooling for hvostid (commitlint, husky). Service code lives in subprojects (Gradle for backend, frontend/ for the SPA).",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
